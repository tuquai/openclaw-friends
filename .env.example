# Blueprint generation uses OpenClaw Gateway as the primary LLM provider.
# If the Gateway is not running, falls back to the OpenAI API key below.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1
OPENCLAW_WORKSPACE_ROOT=
OPENCLAW_HOME=
